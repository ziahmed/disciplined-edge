// Tab bar: dashboard, watchlist, predictions, alerts, settings. See DEVELOPMENT_PLAN.md (User Journey).
import { View } from "react-native";
export default function Screen() {
  return <View>{/* TODO */}</View>;
}
