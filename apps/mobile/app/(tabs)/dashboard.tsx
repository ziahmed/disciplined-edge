// Dashboard — portfolio + risk score + macro board. See DEVELOPMENT_PLAN.md (User Journey).
import { View } from "react-native";
export default function Screen() {
  return <View>{/* TODO */}</View>;
}
