// Root stack: biometric gate on resume, then route to auth/app. See DEVELOPMENT_PLAN.md (User Journey).
import { View } from "react-native";
export default function Screen() {
  return <View>{/* TODO */}</View>;
}
