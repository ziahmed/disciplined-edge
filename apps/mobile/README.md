# mobile (Expo / React Native) — placeholder

Biometric gate uses expo-local-authentication; session/token in expo-secure-store.
Shares the `Prediction` type and api-client with web. Charts via Victory Native.
