import { Injectable } from "@nestjs/common";

/**
 * personas domain logic. Reads/writes the corresponding tables from
 * infra/migrations/0001_init.sql. Stubbed at structure-init time.
 */
@Injectable()
export class PersonasService {}
