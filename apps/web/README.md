# web (Next.js) — placeholder

Phase-1 priority screen: the prediction detail view. It imports the `Prediction`
type from `@disciplined-edge/types` and renders the confidence-band chart with
Recharts. Build this against real ML output for one stock first.
